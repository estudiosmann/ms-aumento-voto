name: CI

on:

  push:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Hola mundo
        run: |
          echo "hola mundo!!"
      - name: Hola mundo 2
        run: |
          echo "hola mundo!! otra vez!!"
